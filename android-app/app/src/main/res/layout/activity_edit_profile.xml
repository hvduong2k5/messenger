<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/ic_back_arrow"
        app:title="Edit Profile"
        app:titleCentered="true"
        app:titleTextAppearance="@style/TextAppearance.MaterialComponents.Subtitle1" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintBottom_toTopOf="@id/btnUpdate"
        app:layout_constraintTop_toBottomOf="@id/toolbar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Avatar Edit Section -->
            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginBottom="32dp">

                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/ivAvatar"
                    android:layout_width="100dp"
                    android:layout_height="100dp"
                    android:src="@drawable/ic_avatar_placeholder"
                    app:civ_border_color="#F2F2F7"
                    app:civ_border_width="2dp" />

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_gravity="bottom|end"
                    android:background="@drawable/bg_settings_icon_circle"
                    android:backgroundTint="@color/messenger_blue"
                    android:padding="8dp"
                    android:src="@android:drawable/ic_menu_camera"
                    app:tint="@color/white" />
            </FrameLayout>

            <TextView style="@style/EditProfileLabel" android:text="What's your name?" />
            <EditText android:id="@+id/etFullName" style="@style/EditProfileInput" android:hint="What's your name?" />

            <TextView style="@style/EditProfileLabel" android:text="Username" />
            <EditText android:id="@+id/etUsername" style="@style/EditProfileInput" android:hint="Username" />

            <TextView style="@style/EditProfileLabel" android:text="Bio" />
            <EditText android:id="@+id/etBio" style="@style/EditProfileInput" android:hint="Bio" android:minLines="1" />

            <TextView style="@style/EditProfileLabel" android:text="Email" />
            <EditText android:id="@+id/etEmail" style="@style/EditProfileInput" android:hint="Email" android:inputType="textEmailAddress" />

            <TextView style="@style/EditProfileLabel" android:text="What is your date of birth?" />
            <FrameLayout android:layout_width="match_parent" android:layout_height="wrap_content">
                <EditText android:id="@+id/etBirthday" style="@style/EditProfileInput" android:focusable="false" android:hint="What is your date of birth?" />
                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="end|center_vertical"
                    android:layout_marginEnd="16dp"
                    android:src="@android:drawable/ic_menu_my_calendar"
                    app:tint="@color/messenger_blue" />
            </FrameLayout>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnUpdate"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_margin="24dp"
        android:text="Update Profile"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:textStyle="bold"
        app:backgroundTint="@color/messenger_blue"
        app:cornerRadius="16dp"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>