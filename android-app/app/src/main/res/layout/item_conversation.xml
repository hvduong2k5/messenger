<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:padding="12dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/iv_conversation_avatar"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_avatar_placeholder"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/ivPresenceStatus"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_margin="2dp"
        android:background="@drawable/bg_online_status"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/iv_conversation_avatar"
        app:layout_constraintEnd_toEndOf="@id/iv_conversation_avatar" />

    <TextView
        android:id="@+id/tv_conversation_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/tv_last_message"
        app:layout_constraintEnd_toStartOf="@+id/tv_timestamp"
        app:layout_constraintStart_toEndOf="@id/iv_conversation_avatar"
        app:layout_constraintTop_toTopOf="@id/iv_conversation_avatar"
        app:layout_constraintVertical_chainStyle="packed" />

    <TextView
        android:id="@+id/tv_last_message"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#888888"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="@id/iv_conversation_avatar"
        app:layout_constraintEnd_toStartOf="@+id/badge_unread_count"
        app:layout_constraintStart_toStartOf="@id/tv_conversation_name"
        app:layout_constraintTop_toBottomOf="@id/tv_conversation_name" />

    <TextView
        android:id="@+id/tv_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#888888"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_conversation_name" />

    <TextView
        android:id="@+id/badge_unread_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:background="@drawable/bg_unread_count"
        android:gravity="center"
        android:minWidth="20dp"
        android:padding="2dp"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_timestamp" />

</androidx.constraintlayout.widget.ConstraintLayout>